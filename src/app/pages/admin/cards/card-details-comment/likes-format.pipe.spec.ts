import { LikesFormatPipe } from './likes-format.pipe';

describe('LikesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LikesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
